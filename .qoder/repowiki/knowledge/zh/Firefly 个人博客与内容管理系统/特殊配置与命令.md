- **包管理**：强制使用 pnpm (`preinstall: npx only-allow pnpm`)。
- **构建流程**：`pnpm build` 会先执行 `scripts/generate-icons.js` 生成图标，再运行 `astro build`，最后通过 `pagefind` 索引静态内容。
- **开发命令**：`pnpm new-post` 用于快速创建新文章模板；`pnpm build-index` 用于构建 AI 搜索向量索引。